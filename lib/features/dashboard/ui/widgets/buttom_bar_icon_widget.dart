import 'package:flutter/material.dart';

class BottomBarIconWidget extends StatelessWidget {
  const BottomBarIconWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
